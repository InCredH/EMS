@page
@model EMS.Pages.Constituents.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Constituent[0].ConstituentName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Constituent) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ConstituentName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ConstituentId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ConstituentId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ConstituentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
