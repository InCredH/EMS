@page
@model EMS.Pages.FSCs.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FSC[0].Element)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.FSC) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Element.ElementId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.FSCId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.FSCId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.FSCId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
